// Purpose: Render legal settings on a stable, shareable route.

import SettingsRoute from "@/components/settings/SettingsRoute"

export default function LegalSettingsPage() {
  return <SettingsRoute activeTab="legal" />
}

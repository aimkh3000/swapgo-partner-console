// Purpose: Render public contacts and working hours on a stable route.

import SettingsRoute from "@/components/settings/SettingsRoute"

export default function ContactSettingsPage() {
  return <SettingsRoute activeTab="contacts" />
}
